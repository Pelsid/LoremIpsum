let burger = document.querySelector("#burger-menu"), menu = document.querySelector("#menuLinks"); function showMenu() { burger.classList.toggle("show"), menu.classList.toggle("show") } const anchors = document.querySelectorAll('a[href*="#"]'); for (let e of anchors) e.addEventListener("click", (function(t) { t.preventDefault(); const l = e.getAttribute("href").substr(1); document.getElementById(l).scrollIntoView({ behavior: "smooth", block: "start" }) })); const quantity = document.querySelectorAll(".order__item").length, slider = document.querySelector("#slider"); let maxWidth = null, left = null, shift = null, interal = !0, cliks = 1; function sliderLeft(e) { 1 !== cliks && (cliks--, interal = !1, left += shift, slider.style.transform = "translateX(" + left + "px)") } function sliderRight(e) { cliks !== quantity ? (cliks++, left -= shift, interal = 1 != e, slider.style.transform = "translateX(" + left + "px)") : cliks === quantity && (cliks = 1, left = 0, slider.style.transform = "translateX(0px)") } window.addEventListener("resize", (function() { window.innerWidth <= 600 ? (shift = 320, interal = !0, maxWidth = quantity * shift, slider.style.width = maxWidth + "px") : window.innerWidth <= 1050 ? (shift = 500, interal = !0, maxWidth = quantity * shift, slider.style.width = maxWidth + "px") : (interal = !1, slider.style.width = "unset", slider.style.transform = "translateX(0px)") })); let timerId = setInterval((() => { !0 === interal && sliderRight() }), 3e3); const select = document.querySelector("#select"), wrapSelect = document.querySelector("#wrap-select"), selectSpan = document.querySelector("#select-span"); wrapSelect.onmouseup = function(e) { select.classList.toggle("_show-select"), selectSpan.classList.toggle("_span-color"),selectSpan.innerHTML = e.target.innerHTML };console.log('https://www.figma.com/file/R76XhMBP5hdvG3fl5TkUI8/Test(1)?t=lnWri1eRPMCy0cJN-0');
